# Traceability matrix

The detailed requirements register is `spec/starfiniti-search-codex-spec/requirements.csv`. This matrix owns delivery state and test evidence; requirement text remains canonical in the specification pack.

| Area | Requirement source | Implementation | Verification | Status |
| --- | --- | --- | --- | --- |
| Provenance and licensing | Product charter §13; licensing spec | `UPSTREAM.md`, audit tool | Deterministic package and tree hashes; clean Plugin Check 2.0.0 policy review | Complete locally; final directory submission remains external |
| Canonical catalog document | Architecture and catalog specs | Domain catalog model and Woo CRUD adapter | Unit fixtures plus real-MariaDB indexing | Qualification implementation |
| Engine-neutral request/response | Contract JSON schemas | `SearchProvider` and REST contract v1.0 | Draft 2020-12 schema compilation, canonical request and negative-bound validation, live installed-response validation, and unit/integration assertions | Qualification implementation |
| Embedded FiboFilters parity | Fork plan phases 1–4 | Pending Gate 2 | Golden DOM/network/URL snapshots | Blocked by licensed semantic configuration |
| Durable synchronization | Sync spec | Per-generation leased outbox, retries, terminal failure/reset, transactional document counts, tokenized self-draining successors, argument-scoped cursor continuations, active/building/ready/previous dual writes, generation-scoped leased shadow builder with authoritative cursor and expired-startup recovery, periodic multi-generation semantic reconciliation and paginated stale cleanup | Real DB update propagation, content/type/status bypass-write repair, ready and rollback window writes, 101-product seed/reconcile and 101-orphan stale continuation regression beside unrelated actions, expired-lease recovery, stale-token denial, retry exhaustion, actual killed-outbox-worker attempt-two reclaim/new-token/exact-once completion, actual shadow-builder mid-document kill/expired reschedule/stale-cursor replay/attempt-two exact completion, and lifecycle tests | Partial; full ingestion/load/large replay/distributed chaos pending |
| Local inverted index | Local engine spec | Schema 3 documents, ordered positional postings, trigram vocabulary, facet values, bounded compiler, versioned analyzer/ranking profile, immutable scoped synonyms/stop words and exact-query curations | SKU/title/phrase/Unicode/typo/synonym/stop-word/pin/boost/bury/hide/rewrite/add-filter/redirect/highlight/explanation/visibility/filter/facet/sort/pagination integration, 100k query-plan benchmark, and 50-sample provider HTTP timing | Partial; strict end-to-end reference latency, Typesense projection, and full-system scale pending |
| Typesense | Typesense spec | Activation-gated adapter, endpoint/credential/name policies, visibility compiler, retry/circuit/mapping, versioned collection/import/alias manager, v30 scoped synonym-set and curation-set reconciler with dynamic query selection and explicit application-only actions | 9 safety/provider scenarios including idempotent drift repair, external-link preservation, incompatible-major denial, and canonical Typesense document-ID binding | Partial; real supported service remains mandatory |
| Migration and rollback | Fork plan phase 7 | Read-only legacy inventory, shadow generations, live ready/previous write targets, synchronized transactional pointer, option/database consistency and exactly-one-active guards, schema/analyzer/profile activation identity | v2-to-v3 build, pending-target activation denial, cutover-window propagation, activate/search, rollback-window propagation, rollback/reactivate, and inconsistent-state fail-closed E2E | Partial; licensed upstream semantic migration pending |
| Storefront surfaces | Storefront spec | Shared no-JS renderer; autocomplete/discovery/Navigation blocks, shortcodes/alias, classic widget, PHP and idempotent headless APIs; focus-managed mobile overlay, typed details disclosure, failed-image recovery, facets, sorting, URL state, safe cards, integration registry, REST | Static race/IME/history/safe-DOM/idempotence/redirect/mobile/details gate, server-render API/widget/navigation/registry contract, live 360 px overlay/focus/disclosure and filter/sort/reload/Back/browser-log evidence | Partial; broad theme/AT/device/zoom/variable-product/cart matrix pending |
| Setup and administration | Storefront/admin spec ADM-001–005 | Fail-closed 12-stage readiness contract in deep health/admin/CLI; exact counts, policy/provider certification, placement, smoke, actionable evidence, immutable relevance preview | Real fixture ready path, forced count-mismatch blocked path, secret scan, admin render, deep CLI output | Qualification implementation; full interactive configuration wizard and external provider connection remain pending |
| Security and operations | Security and MCP specs | Authz/nonces, bounds, visibility, SSRF policy, external secret refs, immutable config/relevance policy, aggregate analytics/privacy, structured redacted logs, uniformly versioned control API, immutable operation engine, official v2 SDK MCP server with exact site registry/fixed control client/tenant-scope policy/untrusted-data envelope/safe JSONL audit, legacy stdio, modern-only HTTP, OAuth metadata, RFC 9068 JWT/JWKS/revocation policy, bounded request controls, private operational endpoints, admin/CLI, backup/restore runbook, scans | Existing WordPress security/operation/DR gates plus strict TypeScript build, nine official v2 MCP protocol/isolation/auth/injection/audit tests including modern `server/discover` and an authenticated HTTP tool call, and live localhost authenticated status/configuration/dry-run planning with temporary credential revocation | Partial; deployed Auth0/TLS/proxy/observability and named-client certification, production backup platform/PITR, query/click/conversion analytics, and distributed chaos remain pending |
| Release qualification | Testing/release spec | Deterministic package, CycloneDX 1.6 SBOM, release manifest, SHA-pinned Windows workflow with one exact-artifact qualifier | Double-build ZIP/SBOM/manifest comparison, WP-CLI overwrite-install, exact file-byte comparison, protected installed contracts including schema-9-to-10 preservation, continuation scheduling, aggregate-storage failure isolation, both forced-process recovery scenarios, and restored-database application smoke, strict live schema validation, and public exact/visibility/discovery smoke | Partial; released-version upgrades, remote CI execution evidence, signing/provenance, and full matrix pending |
