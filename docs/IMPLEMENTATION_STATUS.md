# Implementation status

Last reviewed: 2026-08-09

The project is not enterprise-ready. The label is prohibited until Gate 10 is complete.

| Gate | Outcome | Status | Evidence |
| --- | --- | --- | --- |
| 0 | Provenance, licensing, architecture audit, and golden baseline | Complete locally; licensed parity remains external | Deterministic manifests, dependency inventory, pinned runtimes, clean Plugin Check 2.0.0 policy review, Playground and real-MariaDB baselines; licensed semantic capture remains external |
| 1 | Canonical document and engine contracts | Qualification implementation | Canonical document/unit fixtures, locale-distinct identities, bounded typed custom-field allow-listing, integer money, visibility/scope, versioned provider request/response/capabilities, immutable desired configuration, and automated architecture dependency boundaries |
| 2 | Wrapped embedded filtering behavior with golden parity | Blocked by licensed upstream configuration | Real-MariaDB runtime reaches the legitimate Freemius license boundary; no bypass attempted |
| 3 | Durable outbox and versioned indexing pipeline | Qualification implementation | Real DB lease/retry/dedupe/update-propagation suite; configured batch ceilings that shrink deterministically under memory pressure across seed/outbox/build/reconciliation; expired-lease recovery, stale-token denial, eight-attempt terminal state and explicit reset; actual OS-level PHP worker termination after lease acquisition followed by attempt-two reclaim/new-token/exact-once completion; transactional per-generation document counts; active/building/ready/previous dual writes; argument-scoped Action Scheduler continuations; self-draining outbox successors; paginated multi-generation stale cleanup; and direct content/type/status bypass-write repair |
| 4 | Independent local inverted-index provider | Qualification implementation | Schema 3 ordered positions/trigrams, versioned analyzer/ranking identity, exact SKU/title and phrase search, bounded Unicode typo/synonym recovery, scoped stop words with safe-empty handling, candidate-stage pin/boost/bury/hide, acyclic rewrites, canonical added filters, internal redirects, safe highlights, admin-only explanations/draft preview, filters, facets, sorting, visibility tests, and disposable 100k query-plan benchmark |
| 5 | Typesense provider and provider conformance | Partial; real service blocked | Endpoint/credential/collection safety, bounded query compilation, retry, circuit, redaction, response mapping, canonical document-ID binding, versioned collection creation, per-document NDJSON results, verified idempotent alias activation/rollback, and v30 locale/channel-scoped synonym-set/curation-set reconciliation with dynamic selection, drift repair, read-after-write verification, explicit application-only actions, and legacy-major fail-closed tests pass; Docker/WSL/cloud service conformance remains unavailable |
| 6 | Migration, shadow validation, atomic cutover, and rollback | Partial | Read-only legacy inventory plus tested shadow build, ready-window and retained-rollback dual writes, pending-sync activation refusal, transactional database/option consistency guards, exactly-one-active enforcement, exact count validation, exact-SKU validation, and rollback; semantic mapping pending |
| 7 | Classic, Blocks/FSE, and headless renderers | Qualification implementation | One shared validated renderer across search/discovery/Navigation blocks, shortcodes and alias, classic widget, PHP echo/return APIs, and idempotent headless initializer; no-JS form, accessible autocomplete, focus-managed mobile overlay, keyboard/touch details disclosure, failed-image recovery, faceted cards/filters/sorting/pagination, URL reload/Back state, safe DOM, versioned integration registry/health detection, REST, and live browser evidence; broad theme/AT/device/zoom/variable-product/cart matrix remains |
| 8 | Security, privacy, observability, and operations plane | Qualification implementation | Dedicated least-privilege capabilities/nonces, rate and complexity bounds, server visibility, fail-closed 12-step setup readiness with real catalog/index/placement/smoke evidence, SSRF/credential policy, structured redacted correlation logging, immutable config audit, aggregate-only opt-in analytics, uniformly versioned control responses, immutable operation plans, approval/preconditions/idempotency/audit, admin/CLI routing, and a strict official-SDK MCP stdio adapter with fixed tools, site registration, tenant/scope isolation, server-side credential references, untrusted-data envelopes, safe audit records, mock protocol tests, and live localhost qualification; remote OAuth/JWT transport remains pending |
| 9 | Scale, chaos, accessibility, compatibility, and upgrade qualification | Partial | Real MariaDB functional, accessibility, mobile, artifact-install, v2-to-v3 index upgrade, repeatable schema-9-to-10 metadata upgrade with state/search preservation, outbox lease/retry fault injection plus OS-level worker kill/reclaim, generation-scoped shadow-builder leasing plus OS-level mid-document kill/expired reschedule/stale-cursor replay/attempt-two exact completion, real 101-product cursor scheduling and 101-orphan stale pagination beside unrelated actions, non-polluting disposable 100k query-plan benchmark, and a 50-sample OPcache-enabled HTTP harness with passing provider p50/p95/p99 budgets; strict end-to-end reference-runtime latency, released-version upgrades, full ingestion/load/soak/distributed chaos, and version matrix remain pending |
| 10 | Signed release evidence and rollback rehearsal | In progress | Rollback rehearsal, isolated full-database backup/restore with restored-application exact/visibility smoke, deterministic ZIP, CycloneDX 1.6 SBOM, release manifest, exact checksums, clean official Plugin Check, and a SHA-pinned CI job that must double-build, compare ZIP/SBOM/manifest, install the exact ZIP, byte-compare all files, run protected installed contracts/process recovery/DR/schema validation, and pass public smoke; remote CI execution, signing/provenance, complete gate bundle, and release approval remain |

## Current exit criteria

No enterprise-ready or production-ready label is permitted until every mandatory requirement and Gate 10 has passing evidence. Current blockers are recorded explicitly rather than waived.
