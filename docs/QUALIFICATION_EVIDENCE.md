# Qualification evidence — 2026-08-09

This is development evidence, not a Gate 10 release certificate.

## Pinned environment

- PHP 8.3.28, WordPress 7.0.2, WooCommerce 10.7.0, MariaDB 11.4.10, WP-CLI 2.12.0, Plugin Check 2.0.0.
- Starfiniti localhost: `http://127.0.0.1:8088`.
- Proprietary upstream baseline: `http://127.0.0.1:8089`.
- Exact download and archive hashes are in `config/runtime-lock.json` and `UPSTREAM.md`.

## Passing checks

- The authoritative register accounts for all 115 mandatory requirements: 105 complete, 2 in progress, 4 externally blocked, and 4 disabled/not applicable. Its verifier requires implementation, test, evidence, and release-gate fields; cross-checks `project-status.json`; resolves every referenced local path; and validates all 28 specification-pack hashes.
- The architecture boundary verifier proves 14 domain files are infrastructure-free, both local and Typesense adapters implement the provider-neutral port, and the storefront contains no concrete-provider branch.
- Official WordPress Plugin Check 2.0.0 passes the exact installed ZIP with no errors or warnings. The current policy assessment is recorded in `docs/WORDPRESS_ORG_POLICY_REVIEW.md`.
- Upstream package/tree audit: FiboFilters 841 files, tree `4E50D148170407C48FB46AEEDB654DE05253541B2A144B99DE28546916A2E40A`; FiboSearch Free 447 files, tree `EE0F8FF6D5BFF5A69DBB11179F8072C73C650E91A0C26F179A7A30F88F7AB330`.
- Forty PHP unit tests pass for canonical JSON, integer money, distinct language-document identity, typed custom-field allow-listing, adaptive memory-pressure batches, ordered and repeated Unicode/accent tokenization, numeric-token typing, exact identifiers, bounded Unicode edit distance, position-codec round trips and corruption rejection, scoped/effective synonym expansion, stop-word safe-empty behavior, synonym and rewrite cycle/conflict rejection, provider-native curation-filter and external-redirect denial, deterministic curation priority/action resolution, deterministic semantic documents/checksums, volatile-observation drift exclusion, immutable secret-free configuration, Typesense SSRF/name/credential safety, query compilation, retry, canonical mapping, redaction, circuit breaking, versioned collection creation, canonical document-ID binding, per-document import results, verified idempotent alias activation/rollback, v30 scoped synonym-set/curation-set drift reconciliation and incompatible-major denial, transport-error redaction, immutable operation-plan hashes, generic-operation denial, structured-log secret redaction, and exact aggregate-analytics formulas/null-denominator behavior.
- Nine official v2 SDK MCP tests pass strict TypeScript compilation, modern `server/discover` negotiation, OAuth metadata/challenge behavior, RFC 9068 JWT/JWKS verification, exact issuer/audience/lifetime policy, local `jti` revocation, per-request principal isolation, fixed tool discovery, exact registered control paths, tenant/site/scope denial before network access, dry-run planning, status resources, prompt-injection data containment, secret-key removal, safe audit records, and legacy stdio compatibility. The post-install localhost gate passes authenticated status, secret-free configuration, and dry-run reindex planning, then proves zero Application Passwords remain.
- Ajv 8.18 compiles all six Draft 2020-12 contract schemas in strict mode. The canonical search request and live installed REST response validate; negative cases prove unknown request properties, prohibited filter operators, and protocol-relative redirects fail schema validation.
- Real-MariaDB integration passes schema creation/upgrades, outbox drain, exact SKU, accent folding, visibility exclusions, REST authorization, mutation propagation, and restore.
- A repeatable destructive-in-test schema rehearsal physically copies the table while removing schema-10 generation lease metadata, marks the database as schema 9, reruns the real installer/dbDelta path plus its verified literal-DDL fallback, and proves all four columns return with safe defaults while the active generation, seven documents, immutable configuration checksum, exact SKU result, and restricted-product denial remain unchanged. `ALGORITHM=COPY` prevents MariaDB instant-DDL hidden-column accumulation across repeated qualification runs; three consecutive local cycles passed, and the installer reruns in `finally` for recovery safety.
- Index lifecycle passes schema-v3/analyzer-1/`local-default-v1` shadow build, transactional recorded/actual/source counts, retention of a ready dual-write target, refusal to activate with pending synchronization, exact cutover-window propagation, locked activation with option/database and exactly-one-active guards, exact-SKU search, quoted phrases, safe segment highlights, matched fields, bounded transposition recovery, numeric-SKU non-expansion, stored position decoding, trigram vocabulary, administrator explanation identity, restricted-product typo denial, retained-generation rollback-window propagation, synchronized rollback, inconsistent-pointer fail-closed behavior, reactivation, in-stock filtering, price sort, and filtered facet counts. A v2-to-v3 migration rehearsal passes after preserving and rebuilding through generation boundaries.
- Browser checks pass combobox/listbox semantics, ArrowDown selection, desktop Escape close, and mobile 360 px modal semantics. The mobile run proves one owned full-viewport surface, visible close control, locked/restored body scroll, Shift+Tab containment, Escape from a focused result, opener focus restoration, preserved-query cached reopening, keyboard/touch details disclosure with a named region, failed-image removal, and no browser error logs.
- Dynamic blocks `starfiniti/search`, `starfiniti/discovery`, and the `core/navigation`-constrained `starfiniti/navigation-search` register on WordPress 7.0.2. Server-render tests prove the shortcodes and compatibility alias, classic widget, PHP return/echo APIs, dynamic blocks, and headless initializer share one renderer/component; custom attributes are sanitized, runtime configuration is emitted once, initialization is idempotent, no-JavaScript fallback works, IDs are unique, and ARIA relationships remain valid.
- The versioned storefront integration registry validates at runtime, detects the active Twenty Twenty-Five theme, appears in deep health output, records exact evidence/status/limitations, and leaves Storefront, common builders, and licensed themes explicitly uncertified pending their controlled matrices.
- Live discovery checks pass initial provider-page rendering, changing facet counts, in-stock filtering, stock/category intersection, ascending-price sorting, URL serialization, full reload restoration, browser Back restoration, completed live-region announcements, safe product-card projection, and zero console errors. Static gates prove aborts, monotonically guarded responses, IME composition, history handling, text-only rendering, and prohibited DOM primitive absence.
- Public unsupported filter fields return HTTP 400.
- Anonymous operational status returns HTTP 401/403 while public search remains available.
- Release scan passes with no bundled binaries, archives, private keys, credentials, upstream commercial endpoints, or prefixed upstream runtime namespace.
- Real-MariaDB configuration tests pass immutable monotonic revisions, semantic restore, one-active-revision history, actor/reason audit metadata, and rejection of uncertified external-provider activation.
- The setup readiness contract evaluates all twelve specified stages from live PHP/WordPress/WooCommerce/database compatibility, catalog/public/restricted counts, policy/provider state, storage, locale/variation policy, schema, immutable plan, exact source/index verification, integration-registry plus published placement, and a non-mutating exact SKU/title smoke query. The fixture reports `ready` with `release_certified=false`; a forced generation count mismatch reports `blocked`, then restores exactly. Deep WP-CLI output and the no-JavaScript WooCommerce admin render the same secret-free evidence and next actions.
- The optional customer-facing `starfiniti-search-assistant` is version-matched to the plugin and embedded as eight non-runtime files. Codex and Claude repository entries resolve to this packaged source of truth. Structural validation, relative-reference and repository-leak checks, explicit credential/data refusal, current/planned/unsupported claim boundaries, and eight setup/troubleshooting/storefront/prototype/relevance/theme/security scenarios pass. The administrator page documents both personal installation locations while stating that the assistant does not connect to or modify the site and that this qualification build is not a published release.
- Opt-in analytics tests prove aggregate-only schema/recording, exact weighted formulas and null denominators, provider/configuration/generation context, finite latency coverage, exact inclusive UTC retention, separately authorized no-store reporting, absence of raw-query/user/email/IP columns and query values, purge behavior, privacy callbacks, non-critical search isolation, and restoration to disabled-by-default policy.
- Dedicated capability tests prove only administrators receive Starfiniti health/index/configuration/analytics/migration privileges by default; broad WooCommerce shop-manager authority does not inherit them.
- Reconciliation tests bypass WooCommerce hooks with direct database title and post-type writes, detect semantic checksum drift across validated active/ready/previous write generations, immediately delete an ineligible status through an ordinary durable upsert, paginate multi-generation stale-document deletion, expose bounded reconciliation status, and restore the fixture.
- Action Scheduler scale regression creates 101 real WooCommerce product posts plus 101 synthetic orphan documents while unrelated actions for the same hooks are pending. Seed and catalog-reconciliation workers queue the exact next cursor despite hook/group-wide uniqueness, stale cleanup queues and completes its second batch, and a 20-event outbox worker creates exactly one tokenized successor while work remains. The fixture cancels only its own actions and restores the canonical seven-document/drained-outbox state.
- Outbox fault injection passes expired-lease recovery, rejection of a stale worker token, exact retry-attempt progression, audited terminal failure after eight attempts, explicit re-enqueue/reset, and proof that the active generation is unchanged.
- OS-level process-kill qualification starts a separate PHP/WP-CLI worker, acquires a two-second isolated outbox lease, records its ownership, forcibly terminates that exact verified PHP process, waits for expiry, and proves a second worker reclaims the same event at attempt two with a new token, completes it exactly once, leaves no event residue, and does not change the active generation.
- OS-level shadow-builder termination starts a separate PHP/WP-CLI builder with a two-second generation lease while an unrelated stale generation action is pending, kills the exact verified process after its first durable document but before cursor persistence, proves the database retains state `building`/cursor 0/attempt 1/one document, then verifies startup reschedules the expired build despite Action Scheduler's hook/group-wide uniqueness behavior. Recovery ignores a deliberately stale action cursor, replays idempotently at attempt two, reaches exactly seven unique documents and the expected exact SKU, clears the lease, and never changes the active generation. Cleanup removes only the validated shadow generation and its fixture action.
- Disaster-recovery qualification creates a single-transaction full logical backup, restores it into an isolated PID-scoped MariaDB database, exactly compares row counts for all 13 `wp_sfs_*` tables and all Starfiniti options, then runs the installed application provider against the restored connection. Schema 10, immutable configuration, active generation metadata, seven documents, exact SKU, and restricted-product isolation pass; the disposable database and backup files are containment-checked and removed in `finally`.
- Operation lifecycle tests pass plan immutability, changed-payload rejection, replay-safe execution, explicit approver binding, approval denial, stale-site-state rejection, typed execution, safe failure persistence, and bounded secret-free audit records. Protected REST integration passes anonymous denial, contract versioning, prohibited primitive declarations, planning, execution replay, and audit read.
- Relevance-policy integration passes immutable activation/semantic restore, locale/channel/effective-date scoped directional expansion, deterministic synonym penalty/warning/explanation, stop-word removal, all-stop and punctuation safe-empty handling, candidate-stage pin/boost/bury/hide, acyclic query rewrite, bounded canonical added filter, same-origin redirect output, exact curated totals/facets/pagination, active-filter enforcement on pins and exact SKU, restricted-product synonym/pin denial, and configuration-plan validation. The administrator surface renders escaped synonym/stop-word/curation JSON controls, a mandatory reason, and active-policy explanations. Protected draft preview rejects anonymous callers and cyclic rules, previews pinned and synonym-expanded results plus an internal redirect, returns a deterministic checksum, and proves the active configuration checksum is unchanged.
- Admin and WP-CLI index mutations now use the same plan/approve/execute engine; direct high-impact activation and rollback require human approval identity. Structured WooCommerce logs use correlation IDs and redact raw queries, credentials, authorization values, customer identifiers, and product documents.
- ZIP generation is reproducible across consecutive runs and uses portable forward-slash paths.
- `tools/qualify-artifact.ps1` and the SHA-pinned Windows CI job enforce the complete local artifact chain in one command: two identical plugin ZIP/companion skill ZIP/SBOM/manifest builds, exact WP-CLI overwrite-install, byte comparison of all 82 plugin entries to installed files and all eight companion-skill entries to the embedded installed copy, official Plugin Check with warnings treated as failures, storefront/setup/schema-upgrade/configuration/typed-custom-field/analytics-failure/relevance/control/admin/continuation installed contracts, synchronized cutover/rollback lifecycle, both real process-kill recovery scenarios, isolated full-database restore plus restored-application smoke, strict live JSON Schema validation, and public exact-SKU/restricted-product/discovery smoke.
- CycloneDX 1.6 SBOM and release manifest generation is reproducible and binds the plugin and customer-skill components, source-tree hash, both ZIP hashes, license, runtime requirements, file count, guidance-only/credential-free skill properties, and absence of bundled third-party runtime dependencies. GitHub qualification actions are pinned to immutable commit SHAs.
- The generated ZIP installs over the active plugin through WP-CLI and serves `Blue Alpine Shirt` for exact SKU `EXACT-001`.
- A disposable 100,000-document common-term benchmark on MariaDB 11.4.10 now uses PID-scoped copied-schema tables, one MariaDB session, one warm-up, and 20 server-profile samples; it never inserts into production index tables and verifies that all temporary tables were dropped. Consecutive clean runs measured 6.0/10.7 ms, 4.8/9.9 ms, and 4.5/6.3 ms median/p95, all passing the explicit 60 ms median and 150 ms p95 warm-server budgets. The latest isolated setup took 9,457.9 ms. The plan ranks at most 250 candidates and separately guarantees exact-SKU inclusion. The prior client-process timing method was retired because repeated live-table insert/delete cycles fragmented the qualification database and included process startup rather than server execution.
- The pinned PHP template now loads Zend OPcache instead of merely declaring unused OPcache settings. On the restarted Windows localhost, the repeatable 50-sample REST harness measured provider p50/p95/p99 of 39.9/57.7/131.8 ms, passing the 60/150/300 ms provider budgets. The same run separately measured full HTTP p50/p95/p99 of 324.0/468.6/565.2 ms; those values are recorded as observational and are not misrepresented as a passing end-to-end certification.

## Current artifact

- Version: `0.3.0-alpha.1`.
- File count: 82; uncompressed bytes: 452735.
- Source tree hash: `42A09D134886FC5B1EDFE2C069F83FE3BC7CC79F17B7FC7E3C662ACC5F2B457C`.
- Plugin ZIP SHA-256: `7D1C7E1A3DBFBB7B1D3FF65B1BED260CA2465572779F5F303018A7CC8D963642`.
- Customer skill ZIP SHA-256: `74EC9D359666C545F77A3E1E74853B32F552D7EC058A0AD263EC27407EEDED73`.
- CycloneDX SBOM SHA-256: `9093AF3EA3DD75714F2BFEF58FCA66DD2BAE26CD5DD9EFC67AD061CDDCDD9D6F`.
- Release manifest SHA-256: `62D11C8A6F338D2C6526663118BBF9D4AFFD3C6A943D1C8788C2215442F0143D`.
- Consecutive builds produced the same plugin ZIP, customer skill ZIP, SBOM, and manifest hashes.
- The final ZIP replaced the installed plugin successfully, all 82 installed files byte-match their plugin ZIP entries, and all eight customer skill files byte-match the companion ZIP, with database schema 10 and active local schema 3/analyzer 1/`local-default-v1`. All three dynamic blocks registered; installed-artifact checks passed the shared shortcode/block/widget/PHP/headless renderer contract, integration-registry detection, repeatable schema migration, typed custom-field projection and restoration, exact SKU, quoted phrase, safe text highlight segments, bounded typo recovery, restricted-product and curated-pin denial, discovery-page server rendering, 360 px modal and detail-disclosure controls, failed-image recovery, the 12-step setup readiness/admin/deep-health contract, defined aggregate analytics formulas/retention/privacy/capability separation/storage-failure isolation, 101-record continuation scheduling, ready/previous dual-write cutover and rollback guards, outbox-worker and shadow-builder forced-process recovery, administrator relevance rendering, uniformly versioned control-API authorization/replay, strict JSON Schema response validation, and authorized non-mutating draft synonym/curation preview with pin and internal redirect.

## Non-passing or unavailable gates

- Licensed FiboFilters semantic configuration and golden parity: vendor key unavailable; no bypass attempted.
- Real Typesense conformance/outage testing: supported Docker/WSL runtime unavailable on this Windows host.
- Strict end-to-end autocomplete latency on a production-like reference web runtime: the OPcache-enabled Windows PHP built-in server still measures 324.0/468.6/565.2 ms p50/p95/p99 against 60/150/300 ms budgets. `benchmark:http:strict` fails closed; production PHP-FPM/IIS/Apache evidence remains mandatory.
- 100k WooCommerce ingestion/end-to-end indexing, concurrent-load/soak, broader distributed chaos, broad PHP/WordPress/WooCommerce/browser matrix, multilingual plugins, multisite, and upgrade-from-released-version suites. The current 100k evidence covers the MariaDB autocomplete query plan only; it is not a full-system catalog certification.
- Full semantic FiboFilters mapping, broad theme/assistive-technology/mobile-overlay/cart/detail compatibility, real external-provider credentials/configuration, deployed Auth0/TLS MCP conformance and named-client interoperability, remote CI run evidence, SBOM signing, artifact signature, and final release approval. The deterministic WordPress control API, operation engine, and local modern/legacy MCP implementation are tested; deployed remote authorization is not yet claimed.
